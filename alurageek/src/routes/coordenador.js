export const homePage = (navigate) => {
   return navigate("/")
} ;

export const allProdutosPage = (navigate) => { 
    return navigate("/allProdutos")
};

export const loginPage = (navigate) => { 
    return navigate("/login") 
};

export const addProdutosPage = (navigate) => { 
    return navigate("/adicionarprodutos")
}